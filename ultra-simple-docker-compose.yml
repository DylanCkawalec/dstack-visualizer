version: '3.8'

services:
  tee-trust-validator:
    image: dylanckawalec/ultra-simple-api:latest
    container_name: tee-trust-validator-ultra-simple
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - PYTHONUNBUFFERED=1
    volumes:
      - /var/run/dstack.sock:/var/run/dstack.sock:ro
      - /var/run/tappd.sock:/var/run/tappd.sock:ro
    networks:
      - tee-network
    user: root
    restart: unless-stopped

networks:
  tee-network:
    driver: bridge
